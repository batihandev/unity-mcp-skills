# batch_query_assets

Run a semantic / name / type search for project assets.

> **This recipe is a redirect. Use the native Unity MCP tool instead.**

## Use this instead

**MCP tool:** `Unity_FindProjectAssets`

Pass a natural-language or `t:Type name` query. The MCP tool returns matched asset paths + types.

See `mcp-tools.md` in the repo root for the full MCP tool surface.
